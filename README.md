# codeinterp.com

Source for the CodeInterp Inc. website. A single static page (`index.html`), no build step, served by GitHub Pages from `main`.

To update the site, edit `index.html` and push to `main`; GitHub Pages redeploys within a minute or two.
